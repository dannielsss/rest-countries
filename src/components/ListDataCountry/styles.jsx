import styled from "styled-components";

export const Ul = styled.ul`
  list-style: none;
  font-size: 16px;
  display: flex;
  flex-direction: column;
  gap: 8px;

  li span {
    font-weight: 600;
  }
`